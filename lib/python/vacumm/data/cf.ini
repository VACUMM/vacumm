[variables]

    [[__many__]]

    id=string_list(min=1)
    long_name=string_list
    standard_name=strings
    units=strings
    cmap=cmap(default=None)
    inherit=string
    physloc=string
    atlocs=strings
    axes=dict(default={})

[axes]

    [[__many__]]

    id=string_list(min=1)
    long_name=string_list
    standard_name=strings
    units=strings
    inherit=string
    atlocs=strings
    iaxis=string
    jaxis=string
    axis=string
